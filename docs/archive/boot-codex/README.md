# Boot Codex Archive

Versioned snapshots of the Codex boot file.

Naming convention:
- `boot-codex_v<N>.md`

Rules:
- The active boot file lives at `working-session/boot-codex.md` during sessions.
- Before substantive updates, copy the previous version into this folder with the next version number.
