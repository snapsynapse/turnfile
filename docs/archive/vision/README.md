# Vision Archive

Versioned snapshots of `VISION.md`.

Naming convention:
- `VISION_v<N>.md`

Rules:
- The active version lives at the repo root (`VISION.md`).
- Before substantive edits, copy the previous version into this folder with the next version number.
