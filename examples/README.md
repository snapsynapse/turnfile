# Examples
Examples are evidence and teaching material, not the canonical v1 adoption path.
For fresh adoption, start with `docs/MINIMUM_VIABLE_TURNFILE.md`, `templates/v1-minimal/`, and `tools/turnfile.mjs`.
## Curated current example index
- `turnfile-development/` — session-12-onward development archive index. This is the curated map for the modern multi-agent Turnfile development record.
## Historical evidence
- `inception/` — original sessions 1-11 invention archive. PRD-046 requires this directory to remain present and semantically untouched.
- `inception2/` — transitional working-session archive from early protocol development.
- `ai-feature-tracker/` — first real project use of the predecessor collaboration protocol.
## Rule
Do not make historical examples required reading for v1-minimal conformance. If an example becomes part of the stable teaching path, add a short curated guide that points to the exact files a reader needs.
