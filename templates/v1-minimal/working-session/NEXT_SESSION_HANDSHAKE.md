# Next Session Handshake

This starter file exists so `tools/turnfile.mjs open --root <repo>` can sign a fresh session immediately after `init`.

## Sign-off (next boot)

| Agent | Protocol baseline | Tokenese | Skills | Scope | Heartbeat | Identity enforcing | Signed |
|-------|---|---|---|---|---|---|---|
